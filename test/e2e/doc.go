// Package e2e will hold end-to-end tests against the docker-compose stack
// (MySQL + Minio/S3 + catalog + Trino) for the spike. The spike
// proof is reading back in Trino and watching the old version disappear.
package e2e
