version: "2"

linters:
  default: standard
  exclusions:
    paths:
      - internal/transport/pb

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - internal/transport/pb
